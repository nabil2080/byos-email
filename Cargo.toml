[workspace]
members = ["crypto-core", "packages/crypto-core"]
exclude = ["services/outbound-worker"]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
